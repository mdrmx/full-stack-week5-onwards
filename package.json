{
  "name": "weatherapp-fullstack",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
